# Examples

Put these here when available:

- `tou_cost_example.gh`  a minimal definition: File Path, HB Read Custom Result, the component, a Panel
- `TOU Energy Cost.ghuser`  exported via File, Create User Object, so others can install with one drag
- `screenshot_canvas.png` and `screenshot_report.png`

Do not commit `eplusout.sql` files. They are large and project-specific, and `.gitignore` blocks them.
